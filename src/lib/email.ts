/**
 * Resend + React Email — implement when notifications module is built.
 */
export async function sendEmail(_args: {
  to: string;
  subject: string;
  body: string;
}): Promise<void> {
  throw new Error("sendEmail is not implemented");
}
