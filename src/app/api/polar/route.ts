import { NextResponse } from "next/server";

/** Placeholder — Polar billing webhook / checkout (Module 14). */
export function GET() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}

export function POST() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
